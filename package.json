{
  "name": "jest-runner-bun",
  "version": "0.0.1",
  "description": "Run test in Jest with Bun.",
  "main": "jest-runner-bun/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "jest": "jest",
    "build:js": "node ./scripts/build.mjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bun": "^0.5.7",
    "create-jest-runner": "^0.12.3",
    "jest-circus-bun": "^0.0.1",
    "jest-environment-node": "^29.5.0"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
